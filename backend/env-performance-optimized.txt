# PERFORMANCE OPTIMIZED ENVIRONMENT TEMPLATE
# Copy these settings to your .env file for better performance

# Postgres
DATABASE_URL=postgres://petrol-app:123456@localhost:5432/inflow

# JWT
JWT_SECRET=prfhstadjtlshtlisdhflemtldfsjfgl
JWT_REFRESH_SECRET=nsndfueusfhkuesnfeusnfsenflsfln

# Server
NODE_ENV=development
PORT=9000
ORIGIN_URLS=http://localhost:3000,http://localhost:3001
 
# OPTIMIZED Sequelize pool/tuning for development
DB_POOL_MAX=5              # Reduced from 20 to 5
DB_POOL_MIN=0
DB_POOL_ACQUIRE_MS=5000    # Reduced from 30000 to 5000 (5 seconds)
DB_POOL_IDLE_MS=5000       # Reduced from 10000 to 5000 (5 seconds)
DB_RETRY_MAX=2             # Reduced from 3 to 2

# Controlled schema alter (dev only)
DB_SYNC_ALTER=false

# SQL Logging (disabled for performance)
LOG_SQL=false

# Tenant Resolution Strategy
TENANT_RESOLUTION=host
TENANT_HEADER=x-tenant-id

# Redis (with timeout optimizations)
REDIS_URL=redis://localhost:6379

# PERFORMANCE IMPROVEMENTS MADE:
# 1. Reduced DB_POOL_MAX from 20 to 5 (less connection overhead)
# 2. Reduced DB_POOL_ACQUIRE_MS from 30000 to 5000 (faster timeout)
# 3. Reduced DB_POOL_IDLE_MS from 10000 to 5000 (faster cleanup)
# 4. Reduced DB_RETRY_MAX from 3 to 2 (faster failure detection)
# 5. Redis connection timeout added (3 seconds max)
# 6. Redis retry strategy optimized (max 3 retries, 1 second between)
